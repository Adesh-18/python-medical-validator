#syntax error This line is missing a closing parenthesis. 
# Python raises a SyntaxError because the code doesn't follow proper syntax rules.
#print("Hello, World!"
#name error This line references an undefined variable 'name', which will raise a NameError.
#print(name)
#type error This line attempts to concatenate a string and an integer, which is not allowed in Python and will raise a TypeError.
#age = 5 + "5"
#index error This line tries to access an index that is out of range for the list, which will raise an IndexError.
#numbers = [1, 2, 3]
#print(numbers[5])
#attribute error This line tries to call a method that doesn't exist for the list object, which will raise an AttributeError.
numbers = 42
numbers.append(4)
print(numbers)